Neue App erstellen:
    python3 manage.py startapp "Appname"
    Danach die App in der settings.py unter INSTALLED_APPS eintragen, sonst kommt der Fehler "TemplateDoesNotExist at..."


Publish mit Git:
    git remote set-url origin https://github.com/TomBusse/djangotest.git
    git push --set-upstream origin main
    User: TomBusse
    PW: volle Länge und mit kleinem 'B'




Neustart der Maus in der Virtuellen Maschine:
kill $(ps ax | grep -e "VBoxClient --draganddrop$" | awk '{print $1}'); VBoxClient --draganddrop

